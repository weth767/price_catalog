spring:
  application:
    name: eureka-server
server:
  port: 8761
eureka:
  instance:
    preferIpAddress: true
  client:
    register-with-eureka: false
    fetch-registry: false
  hystrix:
    enabled: true
